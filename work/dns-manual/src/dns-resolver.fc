() recv_internal(slice in_msg) impure {
  ;; do nothing for internal messages
}

int recv_external(slice in_msg) impure {
    int len = in_msg~load_uint(32);
    int str_int = in_msg~load_uint(len);
    return str_int;
}